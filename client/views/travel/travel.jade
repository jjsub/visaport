h1 Travel where ever you like.
.imgdiv(style='background-image:url({{flag}})')

.row
  .col-xs-4
    p Traveling To: <angucomplete id='countryFinder' placeholder='Search a Country' pause='100' selectedObject='country' localdata='allCountry' searchfields='Country' titlefield='Country' minlength='1' />
  .col-xs-4
    form(role='form', novalidate, name='countryForm')
      label Passport from:
        select(ng-model='countryFrom' ng-change='selectCollection(countryFrom)')
          option(value='usavisarequire') USA
          option(value='dominicanvisarequire') Dominican Rep.
  .col-xs-4

.panel.panel-default(ng-show='country')
  h2  Country: {{countryOne}}
  h2 Country: {{country.originalObject.Country}}
  h2 Visa: {{country.originalObject.VisaRequirement}}
  h2 Notes: {{country.originalObject.Notes}}
  .form-group
        label(for='travelTo') Traveling To:
       // input.form-control#travelTo(type='text', name='travelTo', ng-model='countryTo', autofocus, required)
       // button.btn.btn-primary(ng-show='countryForm.$valid', ng-click='findCountry()') Go!
  //.col-xs-9
    //.panel.panel-default(ng-show='country')
    //  h1  Country: {{countryOne}}
    //  h1 Visa: {{country.originalObject.Country}}

   // pre {{country | json}}
    span{{temperature}}
    img(ng-src='{{icon}}')
    //img(ng-src='../assets/image/wings-web1.jpg')

//.panel(ng-repeat='c in allCountry') {{c.Country}}

{{}}
